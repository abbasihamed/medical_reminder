import 'package:flutter/material.dart';

class AddEventScrren extends StatelessWidget {
  const AddEventScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('event'),
    );
  }
}